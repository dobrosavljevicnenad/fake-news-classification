# Fake News Classification (WELFake)

Project for Data Mining / Data Research course.
Dataset: WELFake (title, text, label: 0=fake, 1=real)

## Setup
```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
